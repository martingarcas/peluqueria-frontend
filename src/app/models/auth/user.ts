export interface User {
    id: number;              // User's ID
    name: string;            // User's name
    surname: string;         // User's surname
    email: string;          // User's email
    role: string;           // User's role
    phone: string;          // User's phone number
    registrationDate: Date; // User's registration date
}
